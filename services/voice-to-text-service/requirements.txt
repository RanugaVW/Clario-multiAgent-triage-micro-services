# Voice-to-Text Service (faster-whisper) runtime dependencies.
fastapi==0.141.1
# The [standard] extra brings in the websockets implementation the streaming
# endpoint needs.
uvicorn[standard]==0.53.0
python-multipart==0.0.32
pydantic==2.13.5
numpy==2.5.3
# Pulls in ctranslate2, tokenizers, av (audio decoding) and onnxruntime
# (Silero VAD) transitively - no system ffmpeg required.
faster-whisper==1.2.1
# faster_whisper/utils.py imports `requests` but does not declare it: it used to
# arrive transitively via huggingface-hub, which switched to httpx in 1.0.
# Without this pin `import faster_whisper` fails with ModuleNotFoundError.
requests==2.34.2
